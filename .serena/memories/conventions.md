2-space TypeScript formatting, single quotes, Prettier 100-column style. `src/plugin.ts` must remain default-export-only. Keep behavior docs synchronized across README.md, completion-gate-plugin.md, and skill/completion-gate-config/SKILL.md when plugin behavior changes. Skills use uppercase `SKILL.md`, lowercase kebab-case directory/name, YAML frontmatter with name and description.