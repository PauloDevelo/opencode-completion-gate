OpenCode completion-gate npm plugin. Source: `src/` (plugin/core/config/process), tests: `tests/`, user docs: `README.md`, `completion-gate-plugin.md`, existing skill: `skill/completion-gate-config/SKILL.md`. New project-local skill work belongs under `skill/<name>/SKILL.md`. See `mem:tech_stack` for tooling, `mem:conventions` for implementation conventions, `mem:suggested_commands` and `mem:task_completion` for verification commands.